VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# dummy | live (live must go through Edge Functions — browser keeps dummy)
VITE_ODOO_MODE=dummy
VITE_RAGFLOW_MODE=dummy
VITE_RAGFLOW_DATASET_ID=cms-contracts

# Do NOT put Odoo password / RAGFlow API key in VITE_* (browser).
# Store those in Supabase Edge Function secrets:
#   ODOO_URL, ODOO_DB, ODOO_USERNAME, ODOO_API_KEY
#   RAGFLOW_URL, RAGFLOW_API_KEY
